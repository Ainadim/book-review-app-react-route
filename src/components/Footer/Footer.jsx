import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    return (
        <div>
            <h2>This is Footer</h2>
        </div>
    );
};

Footer.propTypes = {
    
};

export default Footer;